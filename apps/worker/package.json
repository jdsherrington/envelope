{
  "name": "@envelope/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@envelope/core": "workspace:*",
    "@envelope/db": "workspace:*",
    "@envelope/observability": "workspace:*",
    "@envelope/providers-gmail": "workspace:*",
    "@envelope/security": "workspace:*",
    "zod": "^3.24.2"
  }
}
